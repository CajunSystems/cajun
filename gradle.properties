# This file was generated by the Gradle 'init' task.
# https://docs.gradle.org/current/userguide/build_environment.html#sec:gradle_configuration_properties

org.gradle.configuration-cache=true

# Project version - shared across all modules
cajunVersion=0.3.1

# Maven Central Publishing Properties
# Uncomment and set these properties before publishing

# Sonatype credentials
ossrhUsername=${OSSRH_USERNAME}
ossrhPassword=${OSSRH_PASSWORD}

# Signing properties
signing.keyId=${SIGNING_KEY_ID}
signing.password=${SIGNING_PASSWORD}
signing.secretKeyRingFile=${SIGNING_SECRET_KEY_RING_FILE}

# Alternatively, you can use environment variables or the Gradle properties plugin
# to securely provide these credentials
